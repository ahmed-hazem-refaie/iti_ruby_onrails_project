<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table>
  <thead>
    <tr>
      <th>Order for</th>
      <th>From</th>
      <th>Mimg</th>
      <th>Operations</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.order_for, label='Order' %></td>
        <td><%= order.from, label='Resturants' %></td>
        # <td><%= order.mimg %></td>
        <td><%= link_to 'View', order_orderdetails_path(order) %>
        <%= link_to 'Finish', edit_order_path(order) %>
        <%= link_to 'Cancel', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>

<%= link_to 'New Order', new_order_path %>
