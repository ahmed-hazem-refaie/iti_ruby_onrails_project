<p id="notice"><%= notice %></p>

<h1>Orderdetails</h1>

<table>
  <thead>
    <tr>
      <th>Item</th>
      <th>Amount</th>
      <th>Price</th>
      <th>Comment</th>
      <th>User</th>
      <th>Order</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orderdetails.each do |orderdetail| %>
      <tr>
        <td><%= orderdetail.item %></td>
        <td><%= orderdetail.amount %></td>
        <td><%= orderdetail.price %></td>
        <td><%= orderdetail.comment %></td>
        <td><%= orderdetail.user_id %></td>
        <td><%= orderdetail.order_id %></td>
        <td><%= link_to 'Show', order_orderdetail_path(orderdetail) %></td>
        <td><%= link_to 'Edit', edit_orderdetail_path(orderdetail) %></td>
        <td><%= link_to 'Destroy', order_orderdetail_path(orderdetail), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Orderdetail', new_orderdetail_path %>